<UserControl x:Class="SmartX_Telemetry.Views.CommandStreamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Live Asynchronous Telemetry Monitor" Foreground="White" FontSize="20" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,15"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,15">
            <Button Name="BtnStart" Content="Start Background Stream" Background="#4CAF50" Foreground="White" 
                    BorderThickness="0" Padding="15,8" FontWeight="Bold" Click="BtnStart_Click" Cursor="Hand" Margin="0,0,10,0"/>
            <Button Name="BtnStop" Content="Stop Stream" Background="#F44336" Foreground="White" 
                    BorderThickness="0" Padding="15,8" FontWeight="Bold" Click="BtnStop_Click" Cursor="Hand" IsEnabled="False"/>
        </StackPanel>

        <ListBox Name="LstTelemetryLog" Grid.Row="2" Background="#252526" Foreground="#00FF66" 
                 FontFamily="Consolas" FontSize="13" BorderThickness="0" Padding="10"/>
    </Grid>
</UserControl>