<UserControl x:Class="SmartX_Telemetry.Views.IngestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E" Foreground="White">
    <Grid Margin="20">
        <StackPanel Width="400" HorizontalAlignment="Left">

            <TextBlock Text="Device ID:" Foreground="#A6A6A6" Margin="0,10,0,5"/>
            <TextBox Name="TxtDeviceId" Background="#333337" Foreground="White" BorderThickness="0" Padding="8" FontSize="14"/>

            <TextBlock Text="Sensor Value (Double):" Foreground="#A6A6A6" Margin="0,15,0,5"/>
            <TextBox Name="TxtSensorValue" Background="#333337" Foreground="White" BorderThickness="0" Padding="8" FontSize="14"/>

            <TextBlock Text="Deployment Zone:" Foreground="#A6A6A6" Margin="0,15,0,5"/>
            <ComboBox Name="CmbZone" Background="#333337" Foreground="White" BorderThickness="0" Padding="8" FontSize="14">
                <ComboBoxItem Content="Zone A (Hydroponics)" IsSelected="True"/>
                <ComboBoxItem Content="Zone B (Power Grid)"/>
                <ComboBoxItem Content="Zone C (HVAC)"/>
            </ComboBox>

            <TextBlock Text="Attach Diagnostic Log (Media/Log File):" Foreground="#A6A6A6" Margin="0,15,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Name="TxtFilePath" Background="#333337" Foreground="White" BorderThickness="0" Padding="8" FontSize="14" IsReadOnly="True" Grid.Column="0" Margin="0,0,10,0"/>
                <Button Name="BtnBrowse" Content="Browse..." Background="#3E3E42" Foreground="White" BorderThickness="0" Padding="15,8" Grid.Column="1" Click="BtnBrowse_Click" Cursor="Hand"/>
            </Grid>

            <Button Name="BtnSubmit" Content="Transmit to API" Background="#007ACC" Foreground="White" BorderThickness="0" Padding="10" Margin="0,30,0,0" FontSize="16" FontWeight="Bold" Click="BtnSubmit_Click" Cursor="Hand"/>

            <TextBlock Name="TxtStatus" Foreground="#4CAF50" FontWeight="SemiBold" Margin="0,15,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>